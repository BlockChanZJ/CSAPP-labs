#
# trace14.txt - Simple error handling
#
tsh> tsh> ./bogus
tsh> ./bogus: Command not found.
tsh> tsh> ./myspin 4 &
tsh> [1] (124838) ./myspin 4 &

tsh> tsh> fg
tsh> fg command requires PID or %jobid argument
tsh> tsh> bg
tsh> bg command requires PID or %jobid argument
tsh> tsh> fg a
tsh> (a): No such process
tsh> tsh> bg a
tsh> (a): No such process
tsh> tsh> fg 9999999
tsh> fg command requires PID or %jobid argument
tsh> tsh> bg 9999999
tsh> bg command requires PID or %jobid argument
tsh> tsh> fg %2
tsh> %2: No such job
tsh> tsh> fg %1
tsh> 